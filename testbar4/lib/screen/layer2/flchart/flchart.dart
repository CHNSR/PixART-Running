import 'package:flutter/material.dart';
//avg pace each running
class AVGpaceChart extends StatelessWidget {
  const AVGpaceChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}